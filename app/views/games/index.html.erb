<div class="container">
  <p id="notice"><%= notice %></p>

  <h1>Bienvenu sur le Loto</h1>

  <% @games.each do |game| %>
    <div>   
      <%= link_to game.name, game_path(game.id) %>
    </div>
  <% end %>
</div>
